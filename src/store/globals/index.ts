import reducer from './reducer';

import * as actions from './actions';
import * as selectors from './selectors';

export {
  reducer as globalsReducer,
  actions as globalsActions,
  selectors as globalsSelectors,
}
